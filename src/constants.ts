/**
 * Discord application owned by this project, named "Cursor".
 *
 * The name and icon registered against this ID are what Discord renders after
 * "Playing" — this is the only reason a fresh install reads "Playing Cursor"
 * instead of vscord's "Playing Code". Users may override it with the
 * `cursor2discord.applicationId` setting; blank falls back to this.
 */
export const DEFAULT_APPLICATION_ID = "1533739557680320552";
